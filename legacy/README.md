# legacy/

已停用的歷史檔案,僅供參考,不被主程式引用:

- `GameProxyHub.py` — 舊版獨立端口路由工具,功能已併入 `IntegratedApp.py`(Hub 分頁)與 `proxy_core.py`
- `NR_simple.py` — 舊版單檔封裝器,已由 `NetRedirector/` 目錄的模組化 C 核心取代
- `nuitka_packager.py` — 舊版 tkinter 打包 GUI,已由 `build.ps1` 取代

不保證可執行,也不隨發佈打包。
